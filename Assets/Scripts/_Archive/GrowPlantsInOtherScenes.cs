using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GrowPlantsInOtherScenes : MonoBehaviour
{
    private void Start()
    {
        //foreach (Plant[] bench in GlobalControl.Instance.savedValues.GreenhousePlants)
        //{
        //    foreach (Plant plant in bench)
        //    {
        //        StartCoroutine(plant.GrowPlant());
        //    }
        //}
    }
}
